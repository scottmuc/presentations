name: funwithremotes

windows:
  - workspace:
      layout: tiled
      panes:
        - |
          fun_with_remotes
          cd /tmp/git_play/alice
          clear
        - less ~/instructions.txt
        - |
          sleep 5
          cd /tmp/git_play/bob
          banner_watcher "Bob's history" git log --oneline --graph --decorate --all
        - |
          sleep 5
          cd /tmp/git_play/alice
          banner_watcher "Alice's history" git log --oneline --graph --decorate --all
